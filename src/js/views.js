import console from "../views/console";
import labels from "../views/labels";
import users from "../views/labels";
import groups from "../views/labels";
import sessions from "../views/labels";
import home from "../views/home";

export default { console, labels, users, groups, sessions, home };